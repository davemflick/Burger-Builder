import * as actionTypes from '../actions';

const initialState = {
  ingredients: null
}

// const reducer = (curState = initialState, action) => {
//   switch(action.type) {
//     case actionTypes.ADD_INGREDIENT:
//       return {
//         ...curState,
//         counter: curState.price + action.increase
//       };
//   }
 
//   return curState;
// }

export default reducer;